# entry point
port=59075
listen="0.0.0.0"
HOST_IP="202.199.13.164"  # 可选，指定 Worker 的对外 IP 地址，适用于多网卡或容器环境

# 端口池范围，worker 启动的容器会从这个范围里分配端口
port_pool_start=59081
port_pool_end=59100

# cgroup version (v1 or v2)
cgroup_version=1