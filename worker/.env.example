# entry point
port=59075
listen="0.0.0.0"
HOST_IP="202.199.13.164"  # 可选，指定 Worker 的对外 IP 地址，适用于多网卡或容器环境

# 端口池范围，worker 启动的容器会从这个范围里分配端口
port_pool_start=59081
port_pool_end=59082

# cgroup version (v1 or v2)
cgroup_version=1

# script path
gpu_info_script_path="/home/pengyt/projects/neu_box/worker/scripts/info/gpu_info.sh"
npu_info_script_path="/home/pengyt/projects/neu_box/worker/scripts/info/npu_info.sh"
sandbox_script_path="/home/pengyt/projects/neu_box/worker/scripts/sanbox/v2/sandbox.sh"

# device major numbers for sandbox isolation (eBPF device block)
device_major_npu=195
device_major_gpu=235

# sandbox DB directory (persist sandbox state across restarts)
sandbox_db_dir="/home/pengyt/projects/neu_box/worker"